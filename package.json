{
  "name": "minimal-time-helpers",
  "type": "module",
  "version": "0.1.0",
  "description": "minimal time helpers, implemented in TypeScript",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "check_types": "tsc --noEmit --project .",
    "pretest": "tsc --build tsconfig.test.json",
    "test": "node --test --test-reporter spec"
  },
  "keywords": [
    "minimal",
    "time",
    "helpers",
    "date"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/fibo/minimal-time-helpers.git"
  },
  "author": "Gianluca Casati (https://fibo.github.io/)",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.5.1"
  },
  "dependencies": {
    "minimal-type-guard-helpers": "^0.3.0"
  }
}
